# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: criar arquivo .env
      - run: |
          echo "REACT_APP_API_KEY=${{ vars.REACT_APP_API_KEY }}" >> .env
          echo "REACT_APP_API_AUTH=${{ vars.REACT_APP_API_AUTH }}" >> .env
          echo "REACT_APP_API_PROJECT_ID=${{ vars.REACT_APP_API_PROJECT_ID }}" >> .env
          echo "REACT_APP_API_STORAGE_BUCKET=${{ vars.REACT_APP_API_STORAGE_BUCKET }}" >> .env
          echo "REACT_APP_API_MESSAGE_SENDER_ID=${{ vars.REACT_APP_API_MESSAGE_SENDER_ID }}" >> .env
          echo "REACT_APP_API_APP_ID=${{ vars.REACT_APP_API_APP_ID }}" >> .env
      - uses: actions/checkout@v4
      - run: yarn ci && yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PASSWORD_GENERATOR_AS_7C3B6 }}
          channelId: live
          projectId: password-generator-as-7c3b6
